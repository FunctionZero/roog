#pragma once

namespace Global
{
    enum enumGameState
    {
        STATE_MAINMENU
    };

    extern int windowWidth;
    extern int windowHeight;
    extern bool endProgram;

    extern enumGameState gameState;
}
